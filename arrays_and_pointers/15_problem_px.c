#include <assert.h>
#include <stdio.h>
#include <stdlib.h>

void powNxN (unsigned (*A)[N], unsigned k, unsigned m) {

}

int main() {
    int k, m;
    int res = scanf("%d", &k);
    assert(res == 1);
    res = scanf("%d", &m);
    assert(res == 1);


    return 0;
}
