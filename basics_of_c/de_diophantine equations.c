#include <assert.h>
#include <stdio.h>
#include <stdlib.h>



int main()
{
    int a, b, c;

    const int res = scanf("%d %d %d", &a, &b, %c);
    assert(res == 2);


    // printf("%lld %lld %lld", a, b, gcd);
}
