#include <assert.h>
#include <stdio.h>


void solve(int long long N)
{
}


int main()
{
    int long long N = 0;

    const int res = scanf("%lld", &N);
    assert(res == 1);

    solve(N);

    return 0;
}
