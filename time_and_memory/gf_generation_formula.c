#include <assert.h>
#include <stdio.h>

int a, b, max_sequence_length;

void solve(int N)
{

}

int main()
{
    int N = 0;

    const int res = scanf("%d", &N);
    assert(res == 1);

    solve(N);

    printf("%d %d %d", a, b, max_sequence_length);

    return 0;
}
